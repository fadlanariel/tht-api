spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flowmanage_db
    username: postgres
    password: postgres
logging:
  level:
    root: INFO
    com.flowmanage: DEBUG

server:
  error:
    include-stacktrace: never
