spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tht_db
    username: postgres
    password: postgres
logging:
  level:
    root: INFO
    com.fadlan.tht: DEBUG

server:
  error:
    include-stacktrace: never
